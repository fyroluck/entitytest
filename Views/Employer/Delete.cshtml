@model entitytest.Models.Employer

<h1>Delete Employer</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Employer</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.EmployerName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.EmployerName)
        </dd>
    </dl>
</div>

<form method="post">
    <input type="hidden" asp-for="EmployerID" />
    <button type="submit" class="btn btn-danger">Delete</button>
    <a href="@Url.Action("Index", "Employer")" class="btn btn-secondary">Back to List</a>
</form>
